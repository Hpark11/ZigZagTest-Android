<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="shop"
            type="com.zigzagtest.croquiscom.zigzagtest.rankinglist.Shop" />

        <variable
            name="isSection"
            type="Boolean" />

        <variable
            name="rank"
            type="String" />

        <variable
            name="week"
            type="String" />
    </data>

    <FrameLayout
        isSection="@{isSection}"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:tag="@string/string_tag_section_header">

            <TextView
                style="@style/section_header"
                android:text="@{week}"
                tools:text="12월 2주차" />
        </RelativeLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:tag="@string/string_tag_shop_item">

            <TextView
                android:layout_width="52dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginLeft="2dp"
                android:text="@{rank}"
                android:textAlignment="center"
                android:textSize="20sp"
                tools:text="0000" />

            <ImageView
                shopUrl="@{shop.url}"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center_vertical"
                android:layout_marginLeft="6dp"
                android:contentDescription="@string/app_name" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginLeft="10dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/shopNameTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="4dp"
                    android:layout_marginTop="6dp"
                    android:text="@{shop.name}"
                    android:textColor="@color/color_week_text"
                    android:textSize="@dimen/dimen_text_h3"
                    tools:text="임블리" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/ageTextView"
                        style="@style/padded_label"
                        interior="@{shop.representativeAgesData}"
                        android:textSize="@dimen/dimen_text_h6"
                        tools:text="10대" />

                    <TextView
                        style="@style/padded_label"
                        interior="@{shop.firstStyle}"
                        android:layout_marginLeft="4dp"
                        tools:text="@string/string_style_luxury" />

                    <TextView
                        style="@style/padded_label"
                        interior="@{shop.secondStyle}"
                        android:layout_marginLeft="2dp"
                        tools:text="@string/string_style_office_look" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>
    </FrameLayout>
</layout>